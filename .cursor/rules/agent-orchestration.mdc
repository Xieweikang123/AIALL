---
description: Agent 编排分层与通用分类器约束
alwaysApply: true
---

# Agent 编排分层

修改 Agent 编排前，先确认 **tier**（完整路径见 `src/orchestration/orchestrationTiers.ts`）：

| Tier | 用途 | Guard |
|------|------|-------|
| **generic_classifier** | 意图/方案/续跑路由；仅形态与结构 | 禁止词 + 禁止静态个案修复话术 |
| **generic_mechanism** | 准确度、咨询 topic、探索 nudge 契约 | 同分类器 |
| **product** | Ask/Build/Plan prompt、vision、会话审计、`vibeAgent` 装配 | 仅禁止内部组件/功能名 |

- **分类器** → `src/orchestration/generic/`（如 `userIntentClassifiers.ts`）
- **产品 hint** → `src/orchestration/product/`（如 `userIntentHints.ts`）
- `src/services/agentUserIntent.ts` 仅为兼容 barrel，新代码请按 tier 直接 import

## Tier 1：通用分类器

### 适用范围

- `src/orchestration/generic/*`
- `src/services/agentContinuation.ts`、`agentStructuralPatterns.ts`、`agentRunProfile.ts`（路由部分）
- `server/agentRunPolicy.ts`、`server/agentTurnBudget.ts`
- 任何 `*Signal*`、`*Continuation*`、`*RunProfile*` 路由逻辑

### 禁止

- 在 `PLAN_SIGNAL_RE`、路由正则、if/分支里写**当前任务/功能**相关词  
  例：`粘贴图片`、`imageDataUrl`、某页面名、某 API 字段、用户原话里的专有名词
- 用「正文里**出现**某 API/功能名」当作「已是可执行方案」的唯一依据
- 测试 fixture 使用真实需求文案绑定到分类器行为

### 应使用（结构 / 契约，而非话题）

1. **可执行细节**： fenced 代码块，或明确的步骤结构（如 `## 修改方案`、`第一步`）
2. **目标文件**：至少一个可解析的相对路径（`extractPlanFilePaths`）
3. **非征求确认**：排除「是否需要我…」「要不要…」类结尾
4. **（可选）显式标记**：`[PLAN]` 或 `<!-- agent-plan -->`

用户确认执行（`改吧`）与「上一条是否为方案」应分开判断。

## Tier 3：产品编排

- 允许：截图读图流程、`[图已理解]`、Ask/Build/Plan 模式说明、会话 JSON 审计、`AGENTS.md` 路径提示
- 禁止：内部组件名（`FilePanel`、`VibeCodingView` 等，见 `FORBIDDEN_ORCHESTRATION_TERMS`）
- 个案修复：动态 `build*Hint` 注入，勿写「用户要求「xxx」时：常见修复为」进 always-on system prompt

## 测试

- 分类器 fixture：`src/foo.ts`、`featureFlag`、`## 修改方案` + 代码块
- `npm test` 运行 `agentOrchestrationGuard.test.ts` 按 tier 扫描

## Code Review 三问（Tier 1）

1. 这条规则/常量里有没有具体功能名或本次需求的词？
2. 换成 unrelated 需求（如改路由、加重试），规则仍成立吗？
3. 分析回复里**提到**某标识符，会不会误触发？
