---
description: 产品北极星——做 Cursor 类通用 Agent；检索与闭环优先于个案 Playbook
alwaysApply: true
---

# 产品北极星（最核心）

AIALL 的目标是做 **Cursor 类通用编程助手**：会查仓库、会改代码、会验证——而不是「堆了自家 bug 口令的专用脚本」。

**能力主要来自：高精度上下文检索 + 工具闭环 + 模型泛化。**  
**不来自：用 Prompt Engineering 教模型如何修某一类具体 Bug。**

编排细则见 `agent-orchestration.mdc`；本规则是产品级总纲，冲突时以本规则为准。

## 应投入（按优先级）

1. **索引与上下文**：项目事实进上下文（栈 Profile、符号/文件检索、路由与关键文件、按需 knowledge）——用户问「代码里实际怎样」时靠事实，不靠猜话题
2. **Agent 闭环**：改 → read 验证 → 跑测/复现；少靠空口「已修复」
3. **交互体感**：流式、可中断、可续跑、diff 可见——用户天天摸到的「像不像 Cursor」
4. **桌面能力**：Tauri / FS / Git 为真相源（见 `AGENT_SSOT.md`）；禁止再分叉一套 Node 行为实现

## 禁止当作核心竞争力

- always-on / 全局规则里写个案修法（「命中 X → grep Y → patch Z」）
- 把某次 UI/业务 bug（如某浮层、某字段）沉淀成固定函数名、固定 patch 路径
- 用正则猜栈再手写符号清单；应扩 Profile / 索引，让模型据事实选用符号
- 护栏文案直接下令「请 patch 某某逻辑 / 必须 grep 某某符号」——可拦错方向，不可写死修法

## 条件 hint / 护栏怎么写

| 允许 | 禁止 |
|------|------|
| 诊断步骤：查什么、trace 到哪一层 | 具体修法处方：改成哪段代码、搜哪个专有符号 |
| 机制契约：证据深度、探索预算、完成门控 | 话题绑定：用户原话、页面名、API 字段进全局 |
| 拦明显跑偏（如已判定浮层却去搜无关布局词） | 把「该修哪」写成唯一路径 |

## 新增编排前必问

1. 这是在补 **缺失的 ground truth**，还是在 **替模型写排查手册**？
2. 换成无关需求（改路由、加重试），这条规则还成立吗？
3. 去掉这条 hint，补上正确的检索 / Profile / 工具反馈，模型能否自行完成？

后两者为「否」或第 1 条是手册 → **不要合入**，改为上下文注入、检索或工具校验。

## 一句话

**护栏可以留「别乱搜」；永远别写死「该 patch 哪」。**  
沉淀机制与事实注入，不沉淀个案口令。
