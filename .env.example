# AIALL 环境变量说明
#
# 此文件仅供文档参考，不要将真实密钥提交到仓库。
# 应用在 UI 界面中（/ai-config）配置 AI 模型与 API 密钥。

# Tauri 桌面版无需额外环境变量，配置存储在 AppData 中。
# Web 预览模式下（npm run dev:web）以下 API 端点不可用：
#   - Agent / Git / 文件读写（仅桌面版可用）

# 可选：网页抓取代理（用于 /chat 页面 URL 总结）
# WEB_PROXY_URL=http://127.0.0.1:7890
