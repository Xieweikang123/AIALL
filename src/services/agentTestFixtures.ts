/** Generic placeholders for agent orchestration tests — not real audit session text. */

export const FIXTURE_ENUM_LISTING =
  "StatusFlag 枚举：FlagNone=0、FlagPartial=1、FlagFull=2，用于 FooRecord.status 字段。";

export const FIXTURE_PRIOR_DENIAL =
  "**不会。** 切换上下文不更新 `touchMark`。";

export const FIXTURE_CONTRADICTION_USER =
  "但是不知道为啥，切换上下文，选中的项自动跑到上面了";

export const FIXTURE_LOCATE_EVIDENCE =
  "弹窗在 `src/components/foo/BarPanel.vue`，`.dropdown-panel { background: var(--bg-primary); }` 为实色。";

